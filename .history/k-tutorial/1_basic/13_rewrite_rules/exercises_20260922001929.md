* Exercise (Top-level rules): Stored in `lesson-13-ex-toplevel.k`, tested with `ex-toplevel-noop.exp` and `ex-toplevel.exp`
* Exercise (Using top-level rules to evaluate expressions): Stored in `lesson-13-c.k`, tested with `depth.exp`
* Exercise 1: Stored in `lesson-13-ex1-sub.k`, tested with `ex1-sub.exp`
