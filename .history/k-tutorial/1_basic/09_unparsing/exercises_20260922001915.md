* Exercise (Parsing, Execution, and Unparsing / associativity): Stored in `lesson-09-a.k` and `lesson-09-a-right.k`, tested with `exp.bool`
* Exercise (Custom unparsing of terms / format): Stored in `lesson-09-b.k`, `lesson-09-c.k` and `lesson-09-c-ex.k`, tested with `if.stmt`
* Exercise 1: Stored in `lesson-09-ex1.k` (builds on `lesson-09-d.k`), tested with `if.stmt`
* Exercise 2: Stored in `lesson-09-ex2-calculator.k`, tested with `ex2-styled.exp`
