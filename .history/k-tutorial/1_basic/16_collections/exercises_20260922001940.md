* Exercise (Maps / assignment): Stored in `lesson-16-ex-a-assign-update.k` (map update function) and `lesson-16-ex-a-assign-pattern.k` (map pattern), tested with `assign.decl`
* Exercise (Semantic Lists / stack trace): Stored in `lesson-16-ex-b-stacktrace.k`, tested with `trace.func`
* Exercise 1: Stored in `lesson-16-ex1-minilang.k`, tested with `minilang.pgm`
