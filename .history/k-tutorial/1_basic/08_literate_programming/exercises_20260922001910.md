* Exercise (Markdown and K): tested with `plus.int` (README.md itself is the K definition, `--main-module LESSON-08`); commands in `COMMANDS.md`
* Exercise (Markdown Selectors / foo): tested with `foo.int`; commands in `COMMANDS.md`
* Exercise 1: commands in `COMMANDS.md`
* Exercise 2: commands in `COMMANDS.md`
* Exercise 3: Stored in `calculator.md`, tested with `calc.exp`
