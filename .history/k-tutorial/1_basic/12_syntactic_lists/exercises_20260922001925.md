* Exercise (Parsing Syntactic Lists in Programs / concat): Stored in `lesson-12-ex-concat.k`, tested with `concat.strs`; C-call grammar in `lesson-12-e.k`, tested with `call-0.exp`, `call-1.exp`, `call-2.exp`
* Exercise 1: Stored in `lesson-12-ex1-nelist.k`, tested with `sum-empty.ints` (syntax error) and `sum-123.ints`
* Exercise 2: Stored in `lesson-12-ex2-emptysum.k`, tested with `sum-empty2.ints`
