* Exercise (Priority blocks): Stored in `lesson-04-a.k` and `lesson-04-a-alt.k`, tested with `and-or.bool`
* Exercise (Associativity): Stored in `lesson-04-b.k` and `lesson-04-b-alt.k`, tested with `assoc.bool`
* Exercise 1 (dangling else): Stored in `lesson-04-f.k` and `lesson-04-f-alt.k`, tested with `dangling-else.if`
* Exercise 2: Stored in `lesson-04-ex2-explicit.k` and `lesson-04-ex2-inline.k`, tested with `ex2.exp`
* Exercise 3: Stored in `lesson-04-ex3.k`, tested with `ex3.a`
* Exercise 4: Stored in `lesson-04-ex4-original.k` and `lesson-04-ex4.k`, tested with `ex4.expr`
